//#include <reg51.h>
//#include "Delay.h"

//sbit P_INT1 = P3^3;     // INT1 引脚(P3.3)，外部按键输入

//unsigned char led = 0x01;   // 初始流水灯状态：只有最低位亮
//bit dir = 0;                // 0=左移(低位→高位)，1=右移

//// 外部中断1服务程序：切换方向
//void EX1_ISR(void) interrupt 2
//{
//    dir = !dir;             // 切换方向标志
//}

//void main() 
//{
//    // 中断初始化
//    IT1 = 1;            // 1=下降沿触发；0=低电平触发
//    EX1 = 0;            // 先关闭外部中断1
//    EA = 1;             // 开总中断
//    EX1 = 1;            // 使能外部中断1
//    
//    while(1) 
//    {
//        P0 = ~led;      // 共阳：输出0点亮，所以取反
//        
//        DelayMS(300);
//        
//        if(dir == 0)
//        {
//            led = led << 1;     // 左移
//            if(led == 0) 
//                led = 0x01;     // 回到最低位
//        } 
//        else 
//        {
//            led = led >> 1;     // 右移
//            if(led == 0) 
//                led = 0x80;     // 回到最高位
//        }
//    }
//}


////#include <reg52.h>
////#include "Delay.h"

////sbit P_INT1 = P3^3;         // INT1 引脚（P3.3），接按键

////unsigned char led = 0x01;   // 初始流水灯状态
////bit dir = 0;                // 0=左移，1=右移


////// ==================== 外部中断1：按一下，灯动一格（带消抖） ====================
////void EX1_ISR(void) interrupt 2
////{
////    // --- 软件消抖 ---
////    DelayMS(10);            // 延时10ms消抖
////    if(P_INT1 == 1)         // 松手了说明是抖动，直接返回
////        return;
////    
////    // 确认真实按键，执行一步
////    if(dir == 0)
////    {
////        led = led << 1;
////        if(led == 0) led = 0x01;
////    }
////    else
////    {
////        led = led >> 1;
////        if(led == 0) led = 0x80;
////    }
////    
////    P0 = led;
////    
////    while(P_INT1 == 0);     // 等待按键释放
////    DelayMS(10);            // 释放消抖
////}

////void main()
////{
////    P0 = led;
////    
////    IT1 = 1;                // 下降沿触发
////    EX1 = 1;                // 使能外部中断1
////    EA = 1;                 // 开总中断
////    
////    while(1)
////    {
////        // 空等，按键来才动
////    }
////}

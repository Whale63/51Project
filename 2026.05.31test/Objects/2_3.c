//#include <reg51.h>
//#include "Delay.h"

//sbit P_INT0 = P3^2;     // INT0 引脚(P3.2)

//unsigned char led = 0x01;   // 初始流水灯状态：只有最低位亮
//bit dir = 0;                // 0=左移(低位→高位)，1=右移

//// 外部中断0服务程序：切换方向
//void EX0_ISR(void) interrupt 0
//{
//    // 中断里只做最简单的操作：切换方向标志
//    // 消抖放到主循环或改用定时器扫描，中断里不做延时
//    dir = ~dir;  // 或者 dir = !dir;
//}

//void main() 
//{
//    // 中断初始化
//    IT0 = 1;        // 下降沿触发
//    EX0 = 0;        // 先关闭中断，等初始化完成
//    EA = 1;         // 开总中断
//    EX0 = 1;        // 再打开外部中断0
//    
//    while(1) 
//    {
//        P0 = ~led;      // 共阳：输出0点亮，所以取反
//        
//        // 延时期间如果发生中断，会切换dir，下次循环就反向流动
//        DelayMS(300);
//        
//        if(dir == 0)
//        {
//            led = led << 1;     // 左移：0x01→0x02→0x04...
//            if(led == 0) 
//                led = 0x01;     // 到头了，回到最低位
//        } 
//        else 
//        {
//            led = led >> 1;     // 右移：0x01→0x00(处理)→0x80...
//            if(led == 0) 
//                led = 0x80;     // 到头了，回到最高位
//        }
//    }
//}

